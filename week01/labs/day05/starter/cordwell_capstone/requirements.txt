# Week 1 · Day 5 capstone — Cordwell data-cleaning pipeline
# Target: Python 3.13. Plain pip (no uv / pyproject / Ruff).
#
# Install the project itself in editable mode first:
#   pip install -r requirements.txt
#   pip install -e .

pandas>=3.0
numpy>=2.0
pyarrow>=17.0          # Parquet output
pandera>=0.20          # DataFrame schema validation (use: import pandera.pandas as pa)
pydantic>=2.0          # record-level validation (stretch goal)

# --- testing ---
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.0        # property-based tests
