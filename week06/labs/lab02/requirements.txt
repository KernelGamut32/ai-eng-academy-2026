# Week 6 Lab 1.2: Accuracy vs F1 on Imbalanced Data
# Pinned to the cohort standard stack; identical to Lab 1.1's environment,
# so machines set up this morning need nothing new. Install once:
#   pip install -r requirements.txt
#
# Currency notes (verified 2026-08-03):
# - scikit-learn 1.6 is the hard floor for Week 6 Day 1 as a whole
#   (FrozenEstimator landed in 1.6); this lab's stretch 3 uses
#   TunedThresholdClassifierCV, which landed in 1.5. The pin below is
#   the version the lab was executed and verified against.
# - seaborn is intentionally NOT listed. The source version of this lab
#   imported it solely for confusion matrix heatmaps; the lab now uses
#   sklearn's own ConfusionMatrixDisplay, same as Lab 1.1, and drops
#   the dependency.
# - No OpenAI, LangChain, Docker, or local LLM dependencies today.
#   Pure scikit-learn, CPU only.

numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
matplotlib==3.10.8
jupyterlab==4.6.1
ipykernel==7.3.0
