# Lab 09 - Prompt Chaining with LangChain LCEL
# Verified in the build sandbox on Python 3.12; cohort runtime target is Python 3.13.
# Confirm these pins match the cohort image before class.
langchain==1.3.13
langchain-core==1.4.9
langchain-openai==1.3.5
pydantic==2.13.4
openai==2.45.0
python-dotenv==1.2.2
# Notebook execution (usually already present in the cohort image):
# jupyter, ipykernel, nbformat, nbclient
